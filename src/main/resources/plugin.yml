name: LobbyUtils
version: '0.2'
main: net.fritten.lobbyUtils.LobbyUtils
api-version: '1.21'
author: fredo121, Houston9000
description: LobbyUtils Plugin using Spigot API

commands:
  lobby:
    description: Lobby Base Command
    permission: lobby.use
  hub:
    description: Hub Base Command
    permission: lobby.use

permissions:
  lobby.use:
    description: Use Lobby Commands
    default: true